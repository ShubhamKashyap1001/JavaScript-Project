promise = any work doesnot complete now or yet it complete after some time and respond us that work is done or not

promise = The promise object represent the eventual completion(or failure) of an asynchronous operation and its resulting value

A promise  is in one of these states:
. pending : intial state,neither fulfilled nor rejected
. fulfilled: meaning that the operation was completed successfully
. rejected: meaning that the operation failed